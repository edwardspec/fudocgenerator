{{#cargo_query:
tables=research_node
|where=tree="{{{1}}}"
|fields=name,description,unlocksWikitext=unlocks,childrenWikitext=children
|format=template
|named args=yes
|template=One node in research tree
|limit=500
|more results text=
|default=
}}<noinclude>

This template shows the entire page about one Research Tree (list of all nodes in is), e.g. <nowiki>{{All nodes in research tree|Agriculture}}</nowiki>.

[[Category:Cargo templates]]
</noinclude>
