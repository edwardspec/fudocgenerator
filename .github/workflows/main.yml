name: Main testsuite (eslint, luacheck)

on: [push, pull_request, workflow_dispatch]

jobs:
  run-npm-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run "npm test'
        run: |
          npm install
          npm test

